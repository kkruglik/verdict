[package]
name = "verdict-core"
version = "0.1.0"
edition = "2024"

[dependencies]
regex = "1"
thiserror = "2.0.18"
csv = { version = "1.4.0", optional = true }

[features]
default = []
csv = ["dep:csv"]
